import React from 'react';
import Navbar from '../Navbar/Navbar';

const landing = ( props ) => {
    return (
        <div>
           <Navbar></Navbar>
        </div>
    );
};

export default landing;